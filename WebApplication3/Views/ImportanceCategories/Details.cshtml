@model FinanceAnalytic.Models.ImportanceCategory

@{
    ViewData["Title"] = "Подробнее";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
   

    <div>
        <h4>Категория важности</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(Model => Model.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(Model => Model.Name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(Model => Model.NumericEquivalent)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(Model => Model.NumericEquivalent)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.Id" class="button_yellow_s">Редактировать</a> |
        <a asp-action="Index" class="button_yellow_s">Вернуться к списку</a>
    </div>
</div>