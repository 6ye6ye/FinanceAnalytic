@model FinanceAnalytic.Models.SpendingSubCategory

@{
    ViewData["Title"] = "Подробнее";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
    <h1>Подкатегория</h1>

    <div>
        <h4>Подробности</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(Model => Model.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(Model => Model.Name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(Model => Model.SpendingCategory)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(Model => Model.SpendingCategory.Name)
            </dd>

        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.Id" class="button_yellow_s">Редактировать</a> |
        <a asp-action="Index" asp-route-useLogin=true>Вернуться к списку</a>
    </div>
    </div>